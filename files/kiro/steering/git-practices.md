# Git Practices

- Never amend commits and force push. Always create new commits to preserve history.
- Use `git push` normally. If a push is rejected, resolve with a new merge commit, not `--force` or `--force-with-lease`.
